name: SAST Scan
on: [push]

jobs:
  scan:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: ./.github/actions/sast
        with:
          mode: diff
          base_ref: HEAD~1
          strict: false
